#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Disable so CI listens to Gemfile.lock
# ARGV.unshift("--ensure-latest")

ARGV.unshift("--except", "EOLRails") unless ARGV.include?("--except")

load Gem.bin_path("brakeman", "brakeman")
