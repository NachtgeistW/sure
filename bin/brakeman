#!/usr/bin/env ruby
require "rubygems"
require "bundler/setup"

# Disable so CI listens to Gemfile.lock
# ARGV.unshift("--ensure-latest")

ARGV.unshift("--skip-check", "EOLRails") unless ARGV.include?("--skip-check")

load Gem.bin_path("brakeman", "brakeman")
